---
import { siteConfig } from '../site.config';

const year = new Date().getFullYear();
const sourceUrl = import.meta.env.PUBLIC_SOURCE_URL || 'https://github.com/kashiilol/kashiilol.github.io';
---

<footer class="site-footer">
  <div class="footer-inner">
    <span class="footer-owner">© {year} <a href={siteConfig.soundCloudUrl} target="_blank" rel="noreferrer">kashii</a></span>
    <span class="footer-credit">Made with <a href="https://astro.build/" target="_blank" rel="noreferrer">Astro</a> + <a href="https://webtui.ironclad.sh/" target="_blank" rel="noreferrer">WebTUI</a> by <a href="https://github.com/brainage04" target="_blank" rel="noreferrer">brainage04</a></span>
    <a class="footer-source" href={sourceUrl} target="_blank" rel="noreferrer">Source code</a>
  </div>
</footer>

<style>
  .site-footer {
    border-block-start: 1px solid var(--foreground2);
    color: var(--foreground2);
  }

  .footer-inner {
    width: min(100% - 1rem, var(--site-footer-width));
    margin-inline: auto;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: 0.5rem 1rem;
    padding-block: 1rem;
  }

  .footer-owner {
    justify-self: start;
  }

  .footer-credit {
    justify-self: center;
    text-align: center;
  }

  .footer-source {
    justify-self: end;
  }

  a {
    color: var(--foreground1);
    text-underline-offset: 0.18em;
  }

  a:hover {
    color: var(--accent-soft);
  }
  @media (max-width: 700px) {
    .footer-inner {
      grid-template-columns: 1fr;
      justify-items: center;
      text-align: center;
    }

    .footer-owner,
    .footer-credit,
    .footer-source {
      justify-self: center;
    }
  }

</style>
